{
  "name": "rateapi-chrome-extension",
  "version": "1.0.0",
  "description": "Chrome extension showing credit union mortgage rates on Zillow, Redfin & Realtor.com",
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.js",
    "watch": "node esbuild.config.js --watch",
    "clean": "rm -rf dist/*",
    "server": "node server.js",
    "dev": "npm run build && npm run server"
  },
  "dependencies": {
    "lit": "^3.1.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  },
  "engines": {
    "node": ">=18"
  }
}
